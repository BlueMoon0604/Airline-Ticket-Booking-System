server:
  port: 8088

spring:
  application:
    name: notification-service

  amqp:
    deserialization:
      trust:
        all: true

  datasource:
    url: jdbc:mysql://localhost:3306/skybooker_notification_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  mail:
    host: smtp.gmail.com
    port: 587
    username: navyasaxena32@gmail.com
    password: fniq oebp etfh xxbc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  cloud:
    openfeign:
      client:
        config:
          default:
            connectTimeout: 5000
            readTimeout: 5000
            loggerLevel: basic

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true

  instance:
    prefer-ip-address: true
    hostname: localhost

management:
  endpoints:
    web:
      exposure:
        include: health,info

  endpoint:
    health:
      show-details: always

application:
  security:
    jwt:
      secret-key: VGhpc0lzQVN1ZmZpY2llbnRseUxvbmdTYW1wbGVCYXNlNjRLZXlGb3JTa3lCb29rZXI=
  notification:
    email:
      from: navyasaxena32@gmail.com
    sms:
      sender: SKYBKR
  rabbitmq:
    payment-exchange: skybooker.payment.exchange
    payment-queue: skybooker.payment.queue
    payment-routing-key: payment.completed
    flight-exchange: skybooker.flight.exchange
    flight-queue: skybooker.flight.queue
    flight-routing-key: flight.status.updated

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
    tagsSorter: alpha
